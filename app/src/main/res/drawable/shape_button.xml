<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <!-- 填充色，这里会引用选择器，所以先设为透明 -->
    <solid android:color="@android:color/transparent" />
    <!-- 尺寸 -->
    <size
        android:width="64dp"
        android:height="64dp" />
</shape>